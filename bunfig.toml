[install]
# Supply-chain attack safety net: refuse packages published less than 7 days ago.
# Inspired by 2026-05-11 TanStack incident (84 malicious versions published in a 20-minute window).
minimumReleaseAge = "7d"

# Use exact versions in lockfile (no ^ or ~ resolution drift across machines)
exact = false
