/// <reference types="vite-plugin-pwa/client" />

// Injecté au build par Vite (voir `define` dans vite.config.ts) : identifiant
// court du build (MMJJ-HHmm) affiché dans le menu pour vérifier la version live.
declare const __BUILD_ID__: string;
