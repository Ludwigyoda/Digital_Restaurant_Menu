# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# This project is bun-managed (bun.lock). The npm lockfile is generated only
# as a local fallback when bun is not installed — never commit it.
package-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local image backups created during edits (not part of the app)
src/assets/items_backup_*/
